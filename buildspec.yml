version: 0.2

phases:
  pre_build:
    commands:
      - npm run test
  build:
    commands:
      - npm install
      - npm run sls -- deploy -s $STAGE -r $REGION
  post_build:
    commands:
      - npm run acceptance